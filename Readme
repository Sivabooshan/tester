sudo sed -i 's/^#\[multilib\]/[multilib]/; s/^#Include = /Include = /' /etc/pacman.conf && 
sudo pacman -Syyu && 
cd Downloads && 
sudo pacman -S --needed --noconfirm git base-devel && 
git clone https://github.com/Sivabooshan/tester.git && 
cd tester && 
chmod +x tuxmate.sh && 
./tuxmate.sh
